
/* Displays an alert to the visitor. Args:
    - message:    Message to be shown to visitor by alert.*/
function generateAlert(message) {
  window.alert(message);
}

generateAlert("Press OK to continue.");